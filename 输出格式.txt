以下是我根据本次对话中你所有格式要求，总结出的“输出格式指令说明”。你可以直接复制这一段，粘贴到下一次对话的开头，我（DeepSeek）就能继续遵循同样的风格。

---

**输出格式要求：**

1. **三种输出模式**  
   - **LaTeX 模式**：当我要求“输出 LaTeX”时，请给出可直接插入 `.tex` 文档的完整 LaTeX 代码，保留 `\subsubsection*`、`\begin{equation}`、`\begin{multline}` 等环境，段落和公式完整，可含表格。  
   - **普通文本模式**：当我要求“正常显示，不用 LaTeX”时，请用普通文字叙述，数学符号用 `\(...\)` 或 `\[...\]` 括起来，不使用 `\section` 等命令，整体像一篇数学短文。  
   - **Markdown 模式**：当我要求“输出无标题的 md”时，请给出无标题的 Markdown 文本，用 `$$...$$` 或 `\begin{aligned}` 等环境整理公式，文字说明精简，重点在公式推导链。

2. **推导详略要求**  
   - 若说“详细推导”，则给出每一步的变换过程和解释。  
   - 若说“保留少量文字说明”，则仅保留关键步骤，公式占主体。  
   - 若说“直接从……出发推导”，则跳过前序基础，直接从指定表达式开始。

3. **通用要求**  
   - 推导过程按逻辑分步，步骤可编号或自然分段。  
   - 新引入的符号或假设要明确说明（如特征尺度、无量纲数定义）。  
   - 在需要时给出与理想气体或不可压缩情形的对比验证。

4. **示例**  
   - 当我说“补充上转换无量纲方程的过程”，请给出从有量纲方程代入特征尺度、逐项无量纲化的完整推导（LaTeX 模式）。  
   - 当我说“请你将上面的推导保留少量的文字说明，然后使用 gather 或者 align 整理详细公式推导。输出无标题的 md”，则切换为 Markdown 模式，用 align 环境陈列公式，只留必要衔接词。

---

如果下次对话我需要不同的格式，我会在问题中明确指定模式；否则你默认沿用上一次的模式（通常我会指明）。